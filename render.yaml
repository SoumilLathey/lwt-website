# Render Deployment Configuration
services:
  - type: web
    name: lwt-backend
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    rootDir: server
    envVars:
      - key: JWT_SECRET
        value: lwt-super-secret-jwt-key-2024
      - key: EMAIL_USER
        value: latheysoumil@gmail.com
      - key: PORT
        value: 5000
